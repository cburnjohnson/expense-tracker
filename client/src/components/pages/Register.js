import React from 'react';

import RegisterForm from '../register/RegisterForm';

const Register = () => {
    return (
        <>
            <RegisterForm />
        </>
    );
};

export default Register;
